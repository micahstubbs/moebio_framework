function ListOperators(){};
/** 
* receives n arguments and performs addition
*/
ListOperators.getElementFromList=function(list, index){
	if(index<0){
		index = list.length-((-index)%list.length);
	}
	return list[index%list.length];
}
ListOperators.getLengthFromList=function(list){
	return list.length;
}
ListOperators.improveList=function(list){
	
	//TODO: finish method
	return list;
}
ListOperators.createListWithElement=function(element, length){
	var list = new List();
	var n=length==null?1:length;
	var i;
	for(i=0; i<n; i++){
		list.push(element);
	}
	return ListOperators.improveList(list);
}
ListOperators.listAssembler=function(){
	console.log("listAssembler", arguments, Array.isArray(arguments[0]));
	if(Array.isArray(arguments[0])){
		//build Table:
		var table=new Table();
		var i;
		for(i=0; i<arguments.length; i++){
			table.push(arguments[i]);
		}
		console.log("built table:", table);
		return table
	}else{
		//build List:
		var list=new List();
		var i;
		for(i=0; i<arguments.length; i++){
			list.push(arguments[i]);
		}
		console.log("built list:", list);
		return list;
	}
	return null;
}