/* globals mo */
describe("NumberTableOperators", function() {
  it("should normalize to max", function() {
    // var nl1 = new mo.NumberList([1,2,3]);
    // var nl2 = new mo.NumberList([4,5,6]);
    // var nt = new mo.NumberTable.fromArray([nl1,nl2]);

    // var normT = mo.NumberTableOperators.normalizeTableToMax(nt);
    // expect(normT[0].length).toBe(3);
  });
});
